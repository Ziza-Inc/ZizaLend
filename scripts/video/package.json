{
  "name": "zizalend-pitch-video",
  "version": "1.0.0",
  "private": true,
  "description": "Reproducible pipeline that builds the ZizaLend pitch video from the live Testnet deployment",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:draft": "node build.mjs --draft",
    "capture": "node capture.mjs",
    "narration": "node narration.mjs",
    "render": "node render.mjs",
    "verify": "node verify.mjs"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "playwright": "^1.58.2"
  }
}
